<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>AI Data Dashboard Generator</title>
  <style>
    body{font-family:system-ui,Arial,sans-serif;max-width:860px;margin:40px auto;padding:0 16px}
    header{margin-bottom:18px}
    .card{border:1px solid #ddd;border-radius:10px;padding:18px}
    .row{margin:16px 0}
    button{padding:10px 16px;border:0;border-radius:8px;cursor:pointer}
    .primary{background:#111;color:#fff}
    .muted{color:#666}
  </style>
</head>
<body>
  <header>
    <h1>AI Data Dashboard Generator</h1>
    <p class="muted">Upload a CSV → get cleaning steps, useful SQL, and dashboard suggestions.</p>
  </header>

  <section class="card">
    <form action="/upload" method="post" enctype="multipart/form-data">
      <div class="row">
        <input type="file" name="csv_file" accept=".csv" required />
      </div>
      <div class="row">
        <button class="primary" type="submit">Analyze CSV</button>
      </div>
    </form>
  </section>

  <section class="card" style="margin-top:18px">
    <h3>Support the project</h3>
    <p class="muted">Test checkout runs in Stripe test mode.</p>
    <form action="/create-checkout-session" method="post">
      <button type="submit">Buy — $29</button>
    </form>
  </section>

  <!-- Optional: show publishable key is loaded (not required) -->
  <script>
    // "{{ stripe_key | default('') }}";
  </script>
</body>
</html>
